export enum ROUTES {
  HOME = "/",
  TODAY = "/today",
  UPCOMING = "/upcoming",
  ANALYTICS = "/analytics",
  PROJECTS = "/projects",
  TRASH = "/trash",
  SETTINGS = "/settings",
  ADMIN = "/admin",
  LOGIN = "/login",
  SIGNUP = "/signup",
}

export enum API {
  TASKS = "/api/tasks",
}
