// Fallback site assets — empty so sections fall back to the gray placeholder div.
// Upload real images in Directus under the "site_assets" collection using these keys:
//   home_hero         — HeroSection image
//   home_step_1/2/3   — HowItWorks step images
//   product_hero      — Product page hero image
//   about_hero        — About page hero image
export const SITE_ASSETS: Record<string, string> = {};
